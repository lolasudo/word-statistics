version: '3.8'

services:
  backend:
    build: ./backend
    container_name: word-statistics-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/results:/app/results
      - ./backend/uploads:/app/uploads
    environment:
      - MAX_CONCURRENT_TASKS=5
    restart: unless-stopped